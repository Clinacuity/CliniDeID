Javascript demo of the miralium pos-tagger. The model is rather crude to keep the download reasonable. Run ./create_model.sh to regenerate the model.js file.

Either use index.html or include the google gadget in your webpage.
